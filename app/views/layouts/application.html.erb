<!DOCTYPE html>
<html>
<head>
  <title>Basmati Raas</title>
  <link href="http://fonts.googleapis.com/css?family=Dancing+Script" rel="stylesheet" type="text/css">
	<link href="http://fonts.googleapis.com/css?family=Josefin+Sans" rel="stylesheet" type="text/css">
  <%= favicon_link_tag '/assets/br_icon.png', :rel => 'icon', :type => 'image/png' %>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
</head>
<body class="container">
<div class="navbar-wrapper">
	<div class="container">
		<div class = "navbar navbar-inverse">
			<div class="navbar-inner">
				<a class="btn btn-navbar" data-toggle="collapse" data-target=".collapse">  
        		<span class="icon-bar"></span>  
        		<span class="icon-bar"></span>  
        		<span class="icon-bar"></span>  
        		</a>	
				<div class="nav-collapse">
					<ul class="nav nav-tabs">
						<li class="<%= 'active' if current_page?('/static_pages/home') %> dropdown divider-vertical"><%= link_to "Home", controller: "static_pages", action: "home" %></li>
						<li class="<%= 'active' if current_page?('/static_pages/history') %> dropdown divider-vertical"><%= link_to "About Us", controller: "static_pages", action: "history" %></li>
						<li class="<%= 'active' if current_page?('/static_pages/sponsorship') %> dropdown divider-vertical"><%= link_to "Sponsorship", controller: "static_pages", action: "sponsorship" %></li>
						<li class="<%= 'active' if current_page?('/users') %> dropdown divider-vertical"><%= link_to "Team", controller: "users" %></li>
					</ul>
					<ul class="nav nav-tabs pull-right">
						<li class="divider-vertical">
						<% if is_logged_in(session) %>
			  			Welcome, <%= session[:user_hash]["display_name"] %>. <%= link_to "Logout", controller: "static_pages", action: "logout" %>
						<% else %>  
				  		<%= link_to "Login", controller: "static_pages", action: "login" %>
						<% end %>
						</li>				
					</ul>
				</div>
		</div>
	</div>
</div>
<% flash.each do |name, msg| %>
  <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
    <a class="close" data-dismiss="alert">&#215;</a>
    <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
  </div>
<% end %>

<%= yield %>
<div class="linkCenter2">
2013-2014 Basmati Raas
<br></br>
<%= mail_to "brwebmaster@lists.stanford.edu", "Website Contact" %>
</div>
</body>
</html>
